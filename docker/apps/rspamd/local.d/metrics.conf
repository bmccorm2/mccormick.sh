symbol "MIME_HTML_ONLY" {
  score = 0.8;
}

symbol "RCVD_COUNT_ZERO" {
  score = 1.5;
}

symbol "HFILTER_HOSTNAME_UNKNOWN" {
  score = 3.0;
}

symbol "ONCE_RECEIVED" {
  score = 1.0;
}

# No DMARC record at all on the sender's domain is a notable spam signal
symbol "DMARC_NA" {
  score = 1.5;
}

# Sending IP not authorized by sender's SPF record
symbol "R_SPF_SOFTFAIL" {
  score = 3.0;
}
